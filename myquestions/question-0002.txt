Reducción de Dimensionalidad y Varianza Explicada
El Contexto:
Trabajas analizando datos de sensores industriales. Cada máquina tiene decenas de sensores y la cantidad de información es abrumadora. Tu objetivo es aplicar Análisis de Componentes Principales (PCA) para reducir la dimensionalidad de los datos, pero antes necesitas saber cuánta información (varianza) serías capaz de retener si te quedas solo con un número específico de componentes.

Tu Misión:
Implementa una función de Python que reciba un conjunto de datos, seleccione las características de interés, las estandarice y calcule la varianza explicada acumulada tras aplicar PCA. La función debe realizar exactamente los siguientes pasos:

Seleccionar características (Pandas): Extrae del DataFrame original únicamente las columnas especificadas en la lista de características.

Estandarizar los datos (Sklearn): Utiliza StandardScaler para que todas las características tengan media 0 y varianza 1. PCA es muy sensible a la escala.

Aplicar PCA (Sklearn): Instancia y ajusta el modelo PCA utilizando el número de componentes solicitados. Fija el parámetro random_state=42.

Calcular la varianza acumulada (NumPy): Extrae el atributo explained_variance_ratio_ del modelo PCA y utiliza la función de suma acumulada de NumPy para calcular cuánta varianza se explica al ir añadiendo cada componente.

Firma de la función:

Python
def varianza_acumulada_pca(df, columnas_features, n_components):
    # Tu código aquí
    pass
Argumentos esperados:

df: Un objeto DataFrame de la librería pandas con los datos crudos.

columnas_features: Una lista de str con los nombres de las columnas que representan los sensores (características).

n_components: Un int que indica el número de componentes principales a extraer.

Objeto a devolver:

Debe devolver un array de NumPy de una dimensión que contenga la suma acumulada de la varianza explicada para cada componente (por ejemplo, [0.45, 0.70, 0.85]).

Problema 2: Segmentación de Clientes y Métrica de Silueta
El Contexto:
El equipo de marketing te ha pasado un registro de interacciones y compras de clientes. Quieren lanzar una campaña personalizada, por lo que te han pedido agrupar a los clientes en distintos segmentos basándose en ciertas columnas de comportamiento. Quieren saber qué tan bien definidos están esos grupos matemáticamente antes de tomar decisiones.

Tu Misión:
Crea una función que tome los datos, agrupe a los usuarios usando el algoritmo K-Means y evalúe la calidad de esos grupos mediante el "Silhouette Score" (Coeficiente de Silueta). Debes seguir estos pasos:

Filtrar y limpiar (Pandas): Crea un subconjunto del DataFrame quedándote solo con las columnas solicitadas para la segmentación. Elimina cualquier fila que contenga valores nulos (NaN) usando Pandas.

Preparar la matriz (NumPy): Convierte el DataFrame resultante en un array bidimensional de NumPy para alimentar el modelo.

Agrupar los datos (Sklearn): Instancia y ajusta un modelo KMeans con el número de clústeres indicado. Utiliza el parámetro random_state=42 y n_init='auto' (o 10, según la versión) para evitar variaciones en el resultado.

Evaluar los clústeres (Sklearn): Calcula y devuelve el "Silhouette Score" utilizando la matriz de características y las etiquetas predichas por K-Means.

Firma de la función:

Python
def evaluar_segmentacion_kmeans(df, columnas_comportamiento, n_clusters):
    # Tu código aquí
    pass
Argumentos esperados:

df: Un DataFrame de pandas con la información de los clientes.

columnas_comportamiento: Una lista de str con los nombres de las columnas que se usarán para el clustering.

n_clusters: Un int que define cuántos grupos (clústeres) debe formar K-Means.

Objeto a devolver:

Debe devolver un único número de tipo float que represente el valor del Silhouette Score de los grupos formados.
